# Every VITE_ value is public and embedded in the browser bundle.
# Never place passwords, tokens, private keys, or API secrets in this file.
# Leave blank to use the built-in demo adapter and local browser storage.
# Set this to a public REST service origin implementing the documented routes.
VITE_API_URL=
